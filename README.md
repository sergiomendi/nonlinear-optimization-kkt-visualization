# nonlinear-optimization-kkt-visualization
The script computes KKT candidate points for a problem with inequality constraints, checks feasibility, and visualizes the feasible region, constraint boundaries, level curves, and KKT points, highlighting the global optimum.
